#include <stdio.h>
/**
 * main - multiplies two numbers
 * @argc: argument counter
 * @argv: two numbers
 * Return: 0 = success, 1 = error
 */
int main(int argc, char *argv[])
{
	if (argc = 2)
		printf("%d\n", argv[1] * argv[2]);
	else
		printf("error\n");
		return (0);
	return (0);
}
