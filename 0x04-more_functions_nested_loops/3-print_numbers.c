#include "main.h"

/**
 * print_numbers - prints numbers 0 to 9
 * Return: no return
 */

void print_numbers(void)
{
	int i = 48;

	while (i >= 48 && i <= 57)
	{
		_putchar(i++);
	}
	_putchar('\n');
}
