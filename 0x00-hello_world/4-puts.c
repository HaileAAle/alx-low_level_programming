#include <stdio.h>
#include <string.h>
/* printing exactly using puts */
int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle";
/* using puts command */
puts(str);
return (0); }
